language: generic
sudo: required

services:
  - docker

script:
  - docker build .

after_success:
  - docker login -e=$DOCKER_EMAIL -u=$DOCKER_USERNAME -p=$DOCKER_PASSWORD
  - docker push $DOCKER_REPO_NAME 
